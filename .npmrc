# React Native / Expo + Metro require a hoisted node_modules layout to resolve
# react and react-native as singletons across the monorepo.
node-linker=hoisted

# Keep peer dependency resolution predictable across workspaces.
auto-install-peers=true
dedupe-peer-dependents=true
